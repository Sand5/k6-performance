name: K6 Performance Tests (Docker)

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  smoke-tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        environment: [local, qa, prod]

    steps:
      - uses: actions/checkout@v4

      - name: Ensure reports directory exists
        run: mkdir -p k6-tests/reports

      - name: Build Docker images
        run: docker compose build

      - name: Start mock service
        run: docker compose up -d mock-${{ matrix.environment }}

      - name: Wait for mock service
        run: |
          if [ "${{ matrix.environment }}" = "local" ]; then
            PORT=3000
          elif [ "${{ matrix.environment }}" = "qa" ]; then
            PORT=4000
          else
            PORT=5000
          fi

          docker compose run --rm k6 \
            sh -c "npx wait-on http://mock-${{ matrix.environment }}:$PORT"

      - name: Run k6 smoke test
        run: |
          docker compose run --rm --service-ports \
            -e ENV=${{ matrix.environment }} \
            -e DOCKER=true \
            k6 \
            sh -c "npm run k6:smoke:${{ matrix.environment }}:docker"

      - name: Upload k6 HTML Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: k6-smoke-report-${{ matrix.environment }}
          path: k6-tests/reports/smoke-report.html

      - name: Tear down services
        if: always()
        run: docker compose down